# Notification handling: Groups & Pushbullet integration
# notify:
  - name: notify_pushbullet
    platform: pushbullet
    api_key: !secret pushbullet_apikey
  - name: all_devices
    platform: group
    services:
      - service: mobile_app_oneplus8
      - service: mobile_app_redmi_note_8t